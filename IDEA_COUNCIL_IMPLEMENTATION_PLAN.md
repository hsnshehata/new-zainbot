# خطة تنفيذ خاصية «لجنة الأفكار / Idea Council»

## 1. ملخص المنتج والقرارات المعتمدة

**اسم الوثيقة المستهدف:** `IDEA_COUNCIL_IMPLEMENTATION_PLAN.md`  
**مكانها:** جذر مشروع زين بوت.  
**نطاق المهمة:** تخطيط فقط دون تعديل الأكواد أو المنصة القديمة.

الخاصية ستكون أداة مستقلة داخل حساب زين بوت، تسمح لصاحب الحساب بإدخال فكرة مشروع، تنظيمها، ثم تقييمها بواسطة لجنة تحليل متخصصة بالذكاء الاصطناعي. لا تظهر كتجربة شات، بل كمسار قرار منظم ينتج تقريرًا موحدًا، لوحة حقيقة، خطة تحقق مرنة، وMVP مقترح.

القرارات المعتمدة:

- متاحة مبدئيًا لجميع الحسابات دون تمييز بين الباقات.
- الحد: 3 أفكار مكتملة التقييم لكل حساب في الشهر.
- لكل فكرة 3 جولات متابعة.
- البحث السوقي حي وموثق بمصادر حديثة.
- لا توجد مدة ثابتة قدرها 48 ساعة؛ اللجنة تحدد مدة التحقق المناسبة لكل فكرة.
- الأفكار والتقارير خاصة بصاحب الحساب، محفوظة، قابلة للتصدير والحذف، ولا تستخدم في التدريب.
- الخاصية على مستوى الحساب وليست مرتبطة ببوت أو وكيل معين.
- العربية والإنجليزية تُطلقان معًا في نفس التغيير.

### معايير النجاح

يجب أن يخرج المستخدم من كل تقييم بـ:

- قرار واضح: تحقق أولًا، استمر، عدّل الفكرة، غيّر الاتجاه، أو لا تبدأ البناء الآن.
- تفسير للقرار دون الاعتماد على درجة رقمية مجردة.
- أقوى فرصة وأكبر خطر.
- أخطر 3 افتراضات غير مثبتة.
- أهم سؤال يجب حسمه قبل الاستثمار.
- خطة تحقق بمدة ومعيار نجاح يناسبان الفكرة.
- أصغر MVP يمكن اختباره.
- عناصر يجب حذفها أو تأجيلها.
- زاوية تميّز قابلة للدفاع.
- مصادر البحث السوقي وتاريخ الاطلاع عليها.
- لوحة حقيقة قابلة للتحديث بدل تقرير جامد.

---

## 2. تجربة المستخدم والنطاق الوظيفي

### مكان الخاصية

إضافة عنصر جديد في القائمة الرئيسية للوحة التحكم:

- العربية: `لجنة الأفكار`
- الإنجليزية: `Idea Council`
- المسار داخل الواجهة: `page-idea-council`
- الأيقونة المقترحة: مصباح أو مجموعة محللين، وليس أيقونة شات.

الصفحة لا تعتمد على البوت الحالي ولا تتغير عند تغيير الوكيل النشط.

### الشاشة الرئيسية

تعرض:

- عداد الاستخدام الشهري: `2 من 3 أفكار متاحة`.
- زر `اعرض فكرة جديدة`.
- سجل الأفكار السابقة مرتبًا بالأحدث.
- عنوان الفكرة، تاريخها، حالتها، القرار النهائي، وعدد جولات المتابعة المتبقية.
- فلاتر للحالة: مسودة، قيد التنظيم، قيد التقييم، مكتملة، جزئية، فاشلة.
- استكمال المسودات والتقييمات التي توقفت بسبب إغلاق الصفحة.
- حذف الفكرة وتصدير التقرير.
- لا توجد روابط مشاركة عامة في النسخة الأولى.

### المرحلة الأولى: إدخال الفكرة

نموذج مبسط يحتوي على:

- وصف حر للفكرة، من 100 إلى 8,000 حرف.
- السوق أو الدولة المستهدفة، اختياري.
- الفئة المستهدفة، اختياري.
- أكثر شيء يريد المستخدم من اللجنة تقييمه، اختياري.
- لغة التقرير: العربية أو الإنجليزية، والافتراضي لغة لوحة التحكم.
- تنبيه خصوصية يوضح أن النص سيُرسل إلى مزودي AI والبحث المفعّلين في المنصة.
- حفظ تلقائي كمسودة.

تعرض الواجهة أسئلة مساعدة دون إجبار المستخدم على ملء نموذج طويل:

- ما الفكرة؟
- لمن تقدم؟
- ما المشكلة؟
- لماذا قد يغير الناس سلوكهم لاستخدامها؟
- ما الجزء الذي يقلقك أو تريد تقييمه؟

### المرحلة الثانية: بطاقة الفكرة

يقوم `Idea Structurer` بتحويل الوصف إلى بطاقة منظمة تحتوي على:

- عنوان مقترح.
- وصف مختصر.
- العميل المستهدف.
- المشكلة.
- الحل.
- القيمة المقترحة.
- البدائل الحالية.
- السوق أو النطاق الجغرافي.
- نموذج الربح إن كان معروفًا.
- الافتراضات الأولية.
- المعلومات الناقصة.
- سؤال التقييم الأساسي.

يستطيع المستخدم تعديل جميع الحقول قبل بدء اللجنة.

لا تمنع البيانات الناقصة بدء التقييم؛ تُسجل كأمور غير مثبتة وتخفض ثقة التقرير. بدء اللجنة يتطلب تأكيد المستخدم أن البطاقة تمثل فكرته.

المسودات والتنظيم لا تُحسب من الحد الشهري. الحجز يتم فقط عند بدء التقييم الكامل.

### المرحلة الثالثة: جلسة اللجنة

تعرض كتجربة تقدم مرئية:

1. تجهيز بطاقة الفكرة.
2. إجراء البحث السوقي.
3. تحليل أعضاء اللجنة.
4. مقارنة النتائج.
5. إعداد القرار النهائي.

لكل عضو كارت يعرض:

- الاسم والدور.
- الحالة: في الانتظار، يعمل، اكتمل، تعذر.
- ملخص النتيجة بعد الاكتمال.
- زر لفتح مخرجاته المنظمة.

لا تعرض الواجهة التفكير الداخلي أو chain-of-thought؛ تعرض النتائج والأدلة والاستنتاجات فقط.

طلبات التقييم تعمل في الخلفية. يمكن للمستخدم مغادرة الصفحة والعودة دون فقد التقدم.

### أعضاء اللجنة

| العضو | المهمة | المخرجات الإلزامية |
|---|---|---|
| العميل البارد | تقييم سبب تغيير العميل لسلوكه | سبب الاستخدام أو الرفض، البديل الحالي، محفز التجربة |
| المدقق القاسي | كشف الافتراضات والثغرات | 3 افتراضات خطرة، أسئلة صعبة، أضعف نقطة مثبتة |
| خبير التنفيذ | تحديد قابلية البناء والنطاق | تعقيد التنفيذ، MVP لسبعة أيام، وما يجب تأجيله |
| باحث السوق | بحث السوق والبدائل فعليًا | فئة السوق، البدائل، إشارات الطلب، التشبع، والمصادر |
| محامي الشيطان | بناء أقوى حجة ضد المشروع | سبب الفشل الرئيسي، شروط حدوثه، وعلامات التحذير |
| صائد التميّز | اكتشاف زاوية الاختلاف | Unique Wedge، قابلية الدفاع، وسهولة التقليد |
| مصمم التجربة | تصميم أول تجربة استخدام | الرحلة، أول قيمة خلال 60 ثانية، وأكبر احتكاك |
| الداعم الصريح | تحديد ما يستحق الاختبار بواقعية | نقطة القوة، سبب الاستمرار، وما لا يجوز تجاهله |

كل عضو يستلم نفس نسخة بطاقة الفكرة المؤكدة ويعمل مستقلًا دون رؤية نتائج بقية الأعضاء.

### التقرير النهائي

ينتج `Chairperson / Synthesizer` تقريرًا موحدًا يحتوي على:

- ملخص تنفيذي.
- القرار النهائي وتفسيره.
- هل تستحق الفكرة أسبوعًا من البناء: نعم، لا، أو بشروط.
- أقوى فرصة.
- أكبر خطر.
- أخطر 3 افتراضات.
- أهم سؤال يجب الإجابة عنه.
- ما يجب حذفه أو تأجيله.
- خطة تحقق مرنة:
  - الفرضية.
  - الجمهور.
  - خطوات الاختبار.
  - القناة المناسبة.
  - المدة المقترحة.
  - التكلفة التقريبية إن أمكن.
  - معيار النجاح.
  - شرط الإيقاف أو تغيير الاتجاه.
- نطاق MVP لسبعة أيام.
- زاوية التميّز.
- أول Moment of Value.
- نقاط اتفاق اللجنة.
- نقاط الاختلاف وتفسيرها.
- مستوى الثقة وجودة الأدلة.
- مصادر البحث السوقي.

القرار يكون أحد القيم التالية:

- `VALIDATE_FIRST`
- `PROCEED_WITH_CONDITIONS`
- `REFINE`
- `PIVOT`
- `DO_NOT_BUILD_YET`

### لوحة الحقيقة

تظهر بجانب التقرير على الشاشات الكبيرة وتحته على الهاتف.

كل عنصر يتضمن:

- النوع:
  - افتراض.
  - مخاطرة.
  - سؤال.
  - شيء يجب تأجيله.
  - خطة تحقق.
  - إشارة إيجابية.
  - قرار.
- الحالة المعرفية:
  - غير مثبت.
  - مدعوم جزئيًا.
  - مدعوم.
  - تم نفيه.
  - غير منطبق.
- أولوية العنصر.
- سبب التصنيف.
- الإجراء التالي.
- روابط الأدلة إن وجدت.
- حالة العمل التي يعدلها المستخدم:
  - مفتوح.
  - جارٍ التحقق.
  - تم التحقق.
  - مستبعد.
- ملاحظات المستخدم.

تحديث لوحة الحقيقة لا يستهلك جولة AI.

### جولات المتابعة

يسمح بثلاث جولات لكل فكرة، ويختار المستخدم إجراءً واحدًا في كل جولة:

- دافع عن الفكرة أمام اللجنة.
- اقترح Pivot.
- حوّل الفكرة إلى خطة تحقق صغيرة.
- اطلب تصويت اللجنة.
- اسأل عن نقطة واحدة.
- قارن ببديل أو منافس.
- أنشئ خطة MVP لسبعة أيام.

تستخدم كل جولة الأعضاء المناسبين فقط:

- الدفاع: العميل البارد، محامي الشيطان، الداعم الصريح، ثم رئيس اللجنة.
- Pivot: باحث السوق، صائد التميّز، خبير التنفيذ، ثم رئيس اللجنة.
- خطة التحقق: العميل البارد، خبير التنفيذ، مصمم التجربة، ثم رئيس اللجنة.
- التصويت: تصويت مختصر من الأعضاء الثمانية ثم تلخيص الرئيس.
- سؤال محدد: حتى 3 أعضاء مناسبين يظهرون للمستخدم قبل البدء.
- المقارنة: باحث السوق وصائد التميّز مع بحث حي جديد ثم الرئيس.
- MVP: خبير التنفيذ ومصمم التجربة ثم الرئيس.

كل جولة تنتج إصدارًا جديدًا من التقرير وتغييرات واضحة في لوحة الحقيقة، دون الكتابة فوق التقرير الأصلي.

---

## 3. التصميم التقني والعقود العامة

### مكونات البيانات

إضافة مجموعات MongoDB مستقلة:

#### `IdeaProject`

- `userId`
- `encryptedTitle`
- `encryptedOriginalText`
- `encryptedStructuredIdea`
- `outputLanguage`
- `targetMarket`
- `status`
- `version`
- `initialEvaluationsUsed`
- `followupRoundsUsed`
- `latestRunId`
- `createdAt`, `updatedAt`, `deletedAt`

الحالات:

- `DRAFT`
- `STRUCTURING`
- `AWAITING_CONFIRMATION`
- `QUEUED`
- `RUNNING`
- `COMPLETED`
- `PARTIAL`
- `FAILED`
- `CANCELED`

#### `IdeaEvaluationRun`

- `ideaId`, `userId`
- `runType`: `INITIAL` أو `FOLLOW_UP`
- `followupType`
- `status`
- `idempotencyKey`
- `pipelineVersion`
- `ideaSnapshotVersion`
- `currentStage`
- `stageProgress`
- `encryptedFinalReport`
- `encryptedTruthBoard`
- `sourceReferences`
- `usageSummary`
- `errorSummary`
- `workerId`, `heartbeatAt`
- `startedAt`, `completedAt`

#### `IdeaAgentResult`

سجل منفصل لكل عضو أو مرحلة:

- `runId`, `ideaId`, `userId`
- `role`
- `attempt`
- `status`
- `schemaVersion`
- `encryptedInputHash`
- `encryptedOutput`
- `confidence`
- `model/provider`
- `tokenUsage`
- `latencyMs`
- `errorClass`
- `startedAt`, `completedAt`

يوفر هذا الفصل إمكانية إعادة العضو الفاشل فقط دون إعادة اللجنة بالكامل.

#### `IdeaUsageCounter`

مفتاح فريد مركب من `userId + UTC year-month`:

- `initialRunsReserved`
- `initialRunsCompleted`
- `updatedAt`

الحجز ذري لمنع تجاوز 3 تقييمات بسبب الطلبات المتزامنة.

#### `IdeaCouncilConfig`

إعداد وحيد يديره Super Admin:

- `enabled`
- `monthlyIdeaLimit`، افتراضي 3.
- `maxFollowupRounds`، افتراضي 3.
- `workerConcurrency`، افتراضي 2.
- `agentConcurrencyPerRun`، افتراضي 3.
- `researchEnabled`
- `maxSearchCallsPerRun`، افتراضي 4.
- `maxSourcesPerRun`، افتراضي 10.
- حدود النص والتوكن والمهل الزمنية.
- `updatedBy`, `updatedAt`

### واجهات API

جميع المسارات تحت `/api/idea-council` ومحمية بـ`authenticate`. لا يُقبل `userId` من العميل؛ يؤخذ دائمًا من الجلسة.

| الطريقة والمسار | الوظيفة |
|---|---|
| `GET /usage` | الحد الشهري، المستخدم، المتبقي، وموعد إعادة الضبط |
| `GET /ideas` | قائمة أفكار الحساب مع pagination والحالات |
| `POST /ideas` | إنشاء مسودة |
| `GET /ideas/:ideaId` | تحميل الفكرة وتقاريرها لصاحبها |
| `PATCH /ideas/:ideaId` | تعديل المسودة مع optimistic version |
| `DELETE /ideas/:ideaId` | حذف الفكرة وكل نتائجها |
| `POST /ideas/:ideaId/structure` | تشغيل منظم الفكرة |
| `POST /ideas/:ideaId/evaluations` | تأكيد البطاقة وبدء اللجنة |
| `GET /runs/:runId` | حالة التشغيل والتقدم والنتائج المتاحة |
| `POST /ideas/:ideaId/follow-ups` | بدء إحدى جولات المتابعة |
| `POST /runs/:runId/retry` | إعادة المراحل الفاشلة دون استهلاك جديد |
| `POST /runs/:runId/cancel` | إيقاف المراحل التي لم تبدأ |
| `PATCH /ideas/:ideaId/truth-items/:itemId` | تحديث حالة عنصر في لوحة الحقيقة |
| `POST /ideas/:ideaId/feedback` | تقييم فائدة التقرير وتعليق اختياري |
| `GET /ideas/:ideaId/export` | تنزيل Markdown أو صفحة طباعة/PDF |

العقود العامة:

- النجاح المتزامن: `{ success: true, data }`.
- بدء مهمة: HTTP `202` مع `{ runId, status, pollAfterMs }`.
- جميع عمليات البدء تتطلب `Idempotency-Key`.
- أخطاء ثابتة مثل:
  - `IDEA_LIMIT_REACHED`
  - `FOLLOWUP_LIMIT_REACHED`
  - `IDEA_NOT_CONFIRMED`
  - `IDEA_RUN_IN_PROGRESS`
  - `IDEA_NOT_FOUND`
  - `RESEARCH_UNAVAILABLE`
  - `IDEA_COUNCIL_DISABLED`
  - `STRUCTURED_OUTPUT_INVALID`
- أي وصول إلى فكرة لا يملكها المستخدم يرجع 404 لتجنب كشف وجودها.

### تشغيل المهام الطويلة

- لا يبقى طلب HTTP مفتوحًا أثناء اللجنة.
- `POST` ينشئ التشغيل ويرجع 202.
- عامل خلفي يلتقط المهام بـ`findOneAndUpdate` ذري.
- استخدام `RuntimeLease` الحالي بمفتاح `idea-evaluation:<runId>` لمنع تنفيذ المهمة مرتين عند تعدد الحاويات.
- الواجهة تستعلم كل ثانيتين عبر authenticated fetch؛ لا يستخدم SSE لأن التوكن الحالي محفوظ في `localStorage`.
- تجديد heartbeat أثناء التشغيل.
- بعد إعادة تشغيل الخادم تُستأنف المهام ذات الـheartbeat المنتهي.
- حد أقصى افتراضي: تشغيل تقييمين في الوقت نفسه، وثلاثة أعضاء متوازيين داخل كل تقييم.
- مهلة كل استدعاء 60 ثانية، ومحاولة إعادة واحدة للأخطاء المؤقتة أو JSON غير الصالح.
- الحد الكلي للتشغيل 8 دقائق؛ بعدها ينتج تقرير جزئي إن كانت النتائج كافية، أو حالة فشل قابلة لإعادة المحاولة.

### تكامل نظام AI الحالي

- إضافة حالات استخدام:
  - `idea_structuring`
  - `idea_analysis`
  - `idea_market_research`
  - `idea_synthesis`
  - `idea_followup`
- جعل الـorchestrator يقبل `useCase` بدل تثبيته على `general`.
- إضافة `web_search` إلى قدرات كتالوج النماذج.
- استخدام سياسات التوجيه والتشفير والـfailover الحالية بدل مفاتيح جديدة مكشوفة.
- عدم خصم تشغيل اللجنة من عداد رسائل البوت؛ لها حد مستقل، بينما يسجل استهلاك المزود في `AiUsageEvent`.
- توسيع سجل الاستهلاك بحقوق اختيارية:
  - `feature`
  - `operationId`
  - `stage`
  - `agentRole`
  - `searchCalls`
- أي فشل جزئي أو fallback يسجل دون حفظ نص الفكرة في اللوج.

### البحث السوقي

التنفيذ الأول يستخدم محولًا مستقلًا باسم `OpenAiWebResearchAdapter` فوق Responses API:

- أداة `web_search`.
- إرجاع قائمة المصادر.
- `store: false`.
- حد أقصى لأربع عمليات بحث.
- استهداف حتى 10 مصادر.
- استخدام مفتاح OpenAI المشفر المختار بسياسة `idea_market_research`.
- عدم ربط بقية أعضاء اللجنة بمزود OpenAI؛ يظلون تحت نظام التوجيه متعدد المزودين.
- البنية تسمح بإضافة محولات بحث أخرى لاحقًا دون تغيير نماذج البيانات.

البحث يولد حزمة أدلة تشمل:

- عنوان المصدر.
- الرابط.
- النطاق.
- تاريخ النشر إن توفر.
- تاريخ الاطلاع.
- الادعاءات التي يدعمها المصدر.
- درجة جودة المصدر.
- هل المصدر أولي أم ثانوي.

قواعد الجودة:

- تفضيل المصادر الرسمية، صفحات المنتجات، الإحصاءات الأصلية، والأبحاث.
- عدم اعتبار محتوى تسويقي منفرد دليلًا كافيًا.
- فصل الحقائق الموثقة عن استنتاجات اللجنة.
- كل ادعاء سوقي ظاهر يرتبط بمعرف مصدر.
- إذا لم تتوفر 3 مصادر مستقلة مناسبة، يظهر البحث كـ`INSUFFICIENT_EVIDENCE`.
- عند تعطل البحث يستمر باقي التقييم، ويُخفض مستوى الثقة، ولا تُختلق منافسات أو أرقام.
- المصادر تعامل كمدخلات غير موثوقة لمنع prompt injection.
- لا تُخزن نسخ كاملة من صفحات محمية؛ يُحفظ الرابط والبيانات الوصفية والمقتطف الضروري فقط.

الـResponses API يدعم أدوات البحث المدمجة، إرجاع المصادر، وStructured Outputs اللازمة لهذا المسار. ([التوثيق الرسمي](https://developers.openai.com/api/reference/cli/resources/responses/methods/create))

### المخرجات المنظمة

- تعريف JSON Schema مستقل ومُرقّم لكل عضو وللتقرير النهائي.
- استخدام Structured Outputs عندما يدعم النموذج ذلك.
- النماذج الأخرى تستخدم JSON mode ثم تحقق Joi/JSON Schema على الخادم.
- عند فشل التحقق: محاولة إصلاح واحدة، ثم تسجيل العضو كمتعذر.
- لا يقبل الـSynthesizer نصًا حرًا غير متحقق منه.
- رئيس اللجنة لا ينشئ حقائق سوقية جديدة؛ يلخص فقط الأدلة ونتائج الأعضاء.
- كل prompt يحمل `promptVersion`، ويُختبر ضد مجموعة أفكار معيارية عربية وإنجليزية.

---

## 4. الأمان، الخصوصية، الإدارة والقياس

### الخصوصية

- الأفكار لا تظهر إلا لصاحب الحساب.
- Super Admin يرى إحصاءات مجمعة فقط.
- مشاهدة محتوى حساب عميل تتم عبر نظام impersonation الحالي وتُسجل في الـaudit.
- لا تستخدم النصوص أو التقارير لتدريب نماذج زين بوت.
- إرسال البيانات للمزود يتم فقط لغرض التقييم الحالي.
- استخدام `store: false` عند دعمه.
- لا تسجل النصوص أو المخرجات في logger أو Prometheus أو AuditEvent.
- إظهار إشعار موافقة قبل أول تقييم يوضح إرسال البيانات إلى مزودي AI والبحث.

### تشفير البيانات

لأن الأفكار قد تمثل ملكية فكرية:

- تشفير نص الفكرة، البطاقة، مخرجات الأعضاء، التقرير، ولوحة الحقيقة بـAES-256-GCM.
- مفتاح مستقل `IDEA_DATA_ENCRYPTION_KEY` وليس مفتاح مزودي AI.
- تخزين `keyVersion`, `iv`, `authTag`, `ciphertext`.
- دعم تدوير المفتاح وإعادة التشفير تدريجيًا.
- عدم وضع المفتاح في Git أو Docker build args.
- فشل فك التشفير يفشل مغلقًا ولا يعيد بيانات جزئية.

### الحذف والتصدير

- الحذف يزيل الفكرة والتشغيلات ونتائج الأعضاء والتعليقات من قاعدة البيانات النشطة.
- يحتفظ الـaudit بمعرف المورد والعملية فقط دون المحتوى.
- البيانات المحذوفة تخرج من النسخ الاحتياطية خلال مدة أقصاها 30 يومًا، مع إعادة تطبيق سجل الحذف إذا استُعيد Backup أقدم.
- التصدير:
  - Markdown منسق.
  - صفحة طباعة محسنة يمكن حفظها PDF.
  - لا توجد مشاركة عامة أو رابط بدون تسجيل دخول في الإصدار الأول.

### الحماية التشغيلية

- Rate limit خاص بالمسارات إضافة إلى الحد الشهري.
- حد تشغيل واحد نشط لكل فكرة.
- منع الطلبات المتكررة بـIdempotency-Key.
- التحقق من الملكية في كل query.
- تنقية كل نص قبل عرضه، وعدم استخدام `innerHTML` مع مخرجات AI أو عناوين المصادر.
- روابط المصادر تقبل HTTPS فقط وتفتح باستخدام `noopener noreferrer`.
- حدود ثابتة لأحجام النصوص والمخرجات.
- تعليمات الـprompt تعتبر نص المستخدم ومصادر الويب بيانات غير موثوقة.
- فحص المحتوى المخالف قبل اللجنة، مع رسالة واضحة قابلة للترجمة.
- إخلاء مسؤولية بأن التقرير أداة قرار أولي وليس استشارة قانونية أو مالية أو ضمان نجاح.

### لوحة تحكم الإدارة

إضافة قسم `Idea Council` داخل لوحة Super Admin يعرض:

- تشغيل أو إيقاف الخاصية.
- الحد الشهري وعدد جولات المتابعة.
- حدود البحث والتوازي والمهل الزمنية.
- حالة سياسة النماذج الخاصة بكل مرحلة.
- عدد التقييمات المكتملة والجزئية والفاشلة.
- متوسط وP95 للزمن.
- استهلاك التوكن وعمليات البحث حسب المزود.
- نسبة fallback والأخطاء.
- أكثر أنواع المتابعة استخدامًا.
- الحسابات التي اصطدمت بالحد، دون عرض محتوى أفكارها.
- زر إيقاف قبول تقييمات جديدة مع السماح بقراءة التقارير القديمة.

تعليمات الأعضاء وJSON Schemas تكون versioned في الكود في الإصدار الأول ولا تُحرر كنص حر من الإدارة.

### مؤشرات المنتج

تسجيل أحداث دون محتوى حساس:

- `idea_draft_created`
- `idea_structured`
- `idea_evaluation_started`
- `idea_evaluation_completed`
- `idea_evaluation_partial`
- `idea_report_viewed`
- `idea_followup_started`
- `truth_item_updated`
- `idea_report_exported`
- `idea_feedback_submitted`

مؤشرات الإطلاق:

- نسبة الحسابات التي بدأت فكرة.
- نسبة الانتقال من المسودة إلى اللجنة.
- نسبة اكتمال التقييم.
- نسبة فتح التقرير بعد اكتماله.
- نسبة استخدام جولة متابعة.
- نسبة التفاعل مع لوحة الحقيقة.
- تقييم فائدة التقرير.
- العودة للخاصية خلال 7 و30 يومًا.
- التكلفة المتوسطة للتقييم والجولة.

---

## 5. الاختبارات، الإطلاق ومعايير القبول

### اختبارات الخادم

- التحقق من جميع JSON Schemas بالعربية والإنجليزية.
- التأكد أن كل عضو يستلم نفس snapshot ولا يرى نتائج غيره.
- تشغيل الأعضاء الثمانية ثم الـSynthesizer بالترتيب الصحيح.
- اختبار التوازي والمهل وإعادة المحاولة.
- تعطل عضو واحد، عدة أعضاء، البحث، أو الـSynthesizer.
- استئناف التشغيل بعد restart وعدم تكرار الاستدعاءات.
- عدم تنفيذ نفس التشغيل من حاويتين.
- الحجز الذري لحد 3 أفكار شهريًا.
- 3 جولات متابعة فقط حتى مع الطلبات المتزامنة.
- عدم احتساب المسودة أو التنظيم.
- إعادة الحصة عند فشل النظام النهائي، مع احتفاظ إعادة المحاولة بنفس الحجز.
- عزل المستخدمين ومنع IDOR.
- الحذف المتسلسل.
- التشفير، تدوير المفتاح، وفشل فك التشفير.
- عدم تسريب نص الفكرة في logs أو audit أو رسائل الخطأ.
- التحقق من citations وعدم قبول مصدر أو claim غير صالح.
- اختبار prompt injection داخل وصف الفكرة ومقتطفات البحث.
- اختبار امتداد `AiUsageEvent` وسياسات `useCase` والـfailover دون كسر مسارات الشات الحالية.

### اختبارات API

باستخدام Supertest ومزودات AI وهمية:

- إنشاء وتعديل مسودة.
- رفض النص القصير أو الكبير.
- تنظيم الفكرة ثم تعديل البطاقة.
- منع بدء اللجنة قبل التأكيد.
- استجابة 202 وpolling حتى الاكتمال.
- idempotency لنفس الطلب.
- تقرير مكتمل وتقرير جزئي.
- إعادة المراحل الفاشلة.
- إلغاء تشغيل.
- تحديث لوحة الحقيقة.
- تشغيل كل نوع متابعة.
- منع الجولة الرابعة.
- تصدير Markdown.
- حذف الفكرة وعدم إمكان الوصول إليها بعد ذلك.
- دخول Super Admin المباشر مقابل impersonation المدقق.

### اختبارات الواجهة والترجمة

- إضافة جميع النصوص إلى `en` و`ar` داخل `public/js/dashboard_new.js`.
- استخدام `data-i18n`, `data-i18n-placeholder`, و`data-i18n-aria`.
- منع أي نص عربي أو إنجليزي جديد hard-coded في markup أو renderers.
- توسيع `tests/dashboardTranslations.test.js` لتغطية الصفحة الجديدة والنصوص الديناميكية.
- اختبار RTL وLTR.
- الهاتف، التابلت، والشاشات الكبيرة.
- لوحة الحقيقة بجانب التقرير على desktop وتحته على mobile.
- التنقل بلوحة المفاتيح، focus states، labels، وحالات loading/error.
- استعادة الصفحة بعد refresh أثناء التقييم.
- منع XSS من أفكار المستخدم ومخرجات AI وعناوين المصادر.
- اختبار الطباعة والحفظ PDF.

### اختبارات الجودة للذكاء الاصطناعي

إنشاء مجموعة Eval ثابتة تضم على الأقل:

- فكرة Marketplace عربية عامة.
- SaaS B2B بالإنجليزية.
- مشروع محلي بدون نموذج ربح.
- فكرة شديدة الغموض.
- فكرة لها منافسون معروفون.
- فكرة غير قابلة للبناء في أسبوع.
- فكرة تحتوي تعليمات prompt injection.
- فكرة في سوق لا تتوفر عنه مصادر كافية.
- فكرة حساسة قانونيًا أو ماليًا.
- فكرة جيدة يجب ألا تحصل على مدح فارغ.

معايير الجودة:

- وجود المخرجات الإلزامية لكل عضو.
- عدم اختراع أرقام أو منافسين بلا مصدر.
- وضوح الفرق بين الحقيقة والافتراض.
- واقعية الداعم الصريح.
- عدم تحول القرار إلى متوسط آراء سطحي.
- إظهار الاختلافات بين الأعضاء.
- خطة تحقق بمدة مناسبة للفكرة، وليس 48 ساعة ثابتة.
- MVP أصغر من المنتج الكامل فعلًا.
- ثبات مقبول عند إعادة التقييم بنفس المدخل.

### خطة الإطلاق

1. إضافة النماذج والفهارس بشكل additive دون لمس بيانات أو ملفات المنصة القديمة.
2. تجهيز مفتاح تشفير البيانات وسياسات AI والبحث.
3. نشر الخاصية خلف feature flag وهي مغلقة.
4. تشغيل اختبارات migration والفهارس في بيئة staging.
5. تجربة داخلية على أفكار عربية وإنجليزية ومراجعة الجودة والتكلفة.
6. اختبار restart وتعدد الحاويات وتعطل المزود.
7. تفعيل محدود لحسابات داخلية دون تغيير حدود الباقات.
8. بعد اجتياز المعايير، إتاحتها لكل الحسابات بحد 3 أفكار و3 جولات.
9. مراقبة الأخطاء والتكلفة وجودة المصادر خلال أول أسبوعين.
10. تعديل الحدود من لوحة الإدارة إذا ظهرت إساءة استخدام أو تكلفة غير مقبولة.

لا يتطلب الإطلاق downtime. خطة التراجع هي تعطيل قبول تقييمات جديدة مع إبقاء الأفكار والتقارير السابقة قابلة للقراءة.

### التحقق الإنتاجي

لا يكفي نجاح البناء أو `/health`. يجب التحقق من:

- ظهور `لجنة الأفكار / Idea Council` فعليًا في `/dashboard`.
- عمل العربية والإنجليزية وRTL/LTR.
- إنشاء فكرة حقيقية بحساب اختبار.
- حصول API على 202 ثم اكتمال المهمة بعد polling.
- ظهور الأعضاء الثمانية.
- ظهور مصادر بحث قابلة للفتح.
- التقرير ولوحة الحقيقة.
- تنفيذ جولة متابعة.
- صحة العدادات.
- استئناف تقييم بعد إعادة تشغيل حاوية staging.
- عدم ظهور نص الفكرة في اللوج.
- تحميل ملفات JS/CSS الدقيقة المشار إليها من HTML مع cache-busting.

### شروط قبول الإصدار

يعتبر الإصدار جاهزًا فقط عندما:

- تكون الخاصية متاحة لكل حساب نشط.
- يطبق حد 3 أفكار شهريًا و3 جولات ذريًا.
- تعمل رحلة المسودة والبطاقة واللجنة والتقرير والمتابعة كاملة.
- يعمل البحث الحي بمصادر أو يظهر نقص الأدلة بصدق.
- لا يوجد أي افتراض ثابت لتجربة 48 ساعة.
- تبقى الأفكار خاصة ومشفرة وقابلة للحذف والتصدير.
- لا ترتبط الفكرة ببوت معين.
- تنجو المهام من refresh وrestart وتعدد الحاويات.
- تعمل المخرجات المنظمة حتى عند fallback أو فشل جزئي.
- تجتاز اختبارات الخادم والواجهة والأمان والترجمة.
- لا يُمس مجلد المنصة القديمة.
- يتم التحقق من السلوك الإنتاجي الفعلي وليس حالة الحاوية فقط.

## الافتراضات النهائية

- النسخة الأولى تدعم النص فقط؛ الصور والملفات والصوت خارج النطاق.
- لا توجد روابط مشاركة عامة.
- لا يوجد تعاون بين عدة مستخدمين على الفكرة.
- لا يتم إنشاء بوت أو Agent تلقائيًا من التقرير.
- التقارير تُولد بلغة يختارها المستخدم ولا يعاد توليدها عند تغيير لغة الواجهة.
- الشهر المحاسبي للخاصية يعتمد UTC.
- التقييم الفاشل بسبب النظام لا يخصم من الحصة، أما التشغيل المكتمل أو الجزئي القابل للاستفادة فيُحسب.
- إعدادات النماذج والمفاتيح تظل من خلال control plane الحالي.
- البحث الحي يبدأ بمحول OpenAI، مع إبقاء الواجهة الداخلية قابلة لإضافة مزود بحث آخر لاحقًا.
